(function(){const id=new URLSearchParams(location.search).get('id')||location.pathname.match(/chapter-(\d+)/)?.[1]||1;const c=A2Z_getChapter(id);const key='a2z-progress-v1';const read=()=>JSON.parse(localStorage.getItem(key)||'{}');const save=x=>localStorage.setItem(key,JSON.stringify(x));const p=read();p[c.id]??={visited:false,quiz:0,attempts:0,completed:false};p[c.id].visited=true;save(p);document.title=`Chapter ${c.id}: ${c.title} | A2Z Learning Solutions`;document.body.innerHTML=`<main class="wrap"><header><a href="../">← Class 6 Science</a><h1>Chapter ${c.id}: ${c.title}</h1><p>Learn • Practice • Assess • Track progress</p></header><section class="grid"><article class="card"><h2>Learning Hub</h2><p>This chapter follows the A2Z chapter architecture: explanation, summary, key terms, questions, quiz and progress tracking.</p>${c.source?`<a class="btn" href="${c.source}">Open chapter notes →</a>`:'<p><strong>Content preparation:</strong> This chapter is queued for full content production.</p>'}</article><article class="card"><h2>Assessment</h2><p>Chapter score: <strong>${p[c.id].quiz||0}%</strong></p><button id="start">Start Chapter Assessment</button><div id="quiz"></div></article><article class="card"><h2>Progress</h2><div id="progress"></div></article></section></main>`;const qs=[{q:`What is the best way to study Chapter ${c.id}?`,a:['Read, recall, practise and review','Only memorise headings','Skip assessment','Read once'],x:0},{q:'Which action gives useful learning evidence?',a:['Guessing','Taking a quiz and reviewing mistakes','Closing the page','Skipping questions'],x:1},{q:'What should a student do after a weak score?',a:['Stop studying','Review weak concepts and retry','Delete progress','Ignore mistakes'],x:1}];function render(){document.getElementById('progress').innerHTML=`<p>Visited: ${p[c.id].visited?'✓':'—'}</p><p>Attempts: ${p[c.id].attempts}</p><p>Best score: ${p[c.id].quiz}%</p><p>Completed: ${p[c.id].completed?'✓':'Not yet'}</p>`}document.getElementById('start').onclick=()=>{document.getElementById('quiz').innerHTML=qs.map((q,i)=>`<div class="q"><b>${i+1}. ${q.q}</b>${q.a.map((a,j)=>`<label><input type="radio" name="q${i}" value="${j}"> ${a}</label>`).join('')}</div>`).join('')+'<button id="submit">Submit assessment</button>';document.getElementById('submit').onclick=()=>{let s=0;qs.forEach((q,i)=>{if(document.querySelector(`input[name=q${i}]:checked`)?.value==q.x)s++});const score=Math.round(s/qs.length*100);p[c.id].quiz=Math.max(p[c.id].quiz,score);p[c.id].attempts++;p[c.id].completed=score>=60;save(p);document.getElementById('quiz').insertAdjacentHTML('afterbegin',`<div class="result"><strong>Score: ${score}%</strong> — ${score>=60?'Chapter completed!':'Review the chapter and try again.'}</div>`);render()}};render()})();
